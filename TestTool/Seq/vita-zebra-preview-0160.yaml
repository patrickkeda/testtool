version: '1.0'
metadata:
  name: vita-zebra-preview-0160
  description: '预览0160历史打印文件（不实际打印）'
  author: 'TestTool'
  created_at: '2026-04-30T14:03:00.000000'
  product: Vita01
  station: LABEL
  version: '1.0'

variables:
  zebra_printer_name: 'ZDesigner 110Xi4 600 dpi (副本 1)'

steps:
  - id: step_1
    name: Preview0160Label
    type: printer.zebra
    params:
      channel: local
      printer_name: '${zebra_printer_name}'
      copies: 1
      zpl_file: 'Result/print_preview/8010005BHQ26DSA0160_step_5.zpl'
      encoding: 'utf-8'
      timeout_ms: 3000
      preview: true
      preview_only: true
      save_preview: false
    timeout: 20000
    retries: 1
    condition: null
    expect: null
    on_failure: fail
    at_config: null
    state_measurement_config: null
    manual_judgment_config: null

timeout: null
retries: 0
on_failure: stop
