# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# TestTool specific
# 日志文件（Result 整目录已忽略；此处保留通配便于其它路径）
*.log

# 配置文件中的敏感信息（可选，根据实际情况调整）
# TestTool/Config/config.yaml

# ---------------------------------------------------------------------------
# 运行产物与打包输出 — 不提交（本地/流水线生成；减小仓库体积）
# ---------------------------------------------------------------------------
# 测试与标定等输出：ZPL 预览、上传 JSON、红外图、标定 mcap/pcd 等
# 例外：字体/矢量资源需随仓库分发时保留（可日后迁到 assets/ 再去掉例外）
TestTool/Result/**
!TestTool/Result/font_sources/
!TestTool/Result/font_sources/**

# 工作目录错误时产生的嵌套副本（如 Result 写在 TestTool 子目录下）
TestTool/TestTool/

# PyInstaller 构建目录与中间产物（保留 build/*.spec、build.ps1、文档等）
TestTool/build/pyinstaller/
TestTool/build/TestTool/
TestTool/build/TestTool_Package_build/

# 编译文件
*.pyc
*.pyo
*.pyd

# 临时文件
*.tmp
*.bak
*.swp
*.swo

# 系统文件
Thumbs.db
Desktop.ini

# 二进制文件
TestTool/client/bin/*
!TestTool/client/bin/.gitkeep

# 数据文件
*.data
*.db
*.sqlite
*.sqlite3

# 加密密钥文件（如果有）
*.key
*.pem
*.crt

# 备份文件
*.backup
*_backup.*

# 压缩文件
*.rar
*.zip
*.7z
*.tar.gz
